[project]
name = "muve-data-pipeline"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "dlt[postgres]>=1.12,<2",
  "snowflake-connector-python>=3.15,<4",
  "sqlmesh>=0.190,<1",
]

[project.scripts]
muve-pipeline = "muve_pipeline.runner:main"

[tool.pytest.ini_options]
pythonpath = ["."]
